import '../css/main-page__item.css';

const Home = ({ ref, inView }) => (
    <div ref={ref} className='main-page__item main-page__item_home'>
        <h1 className="main-page__item_home__logo">R</h1>
    </div>
);

export default Home;